<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Soto Analyzer - Upload Foto Soto</title>
    <meta name="description" content="Upload foto soto untuk mendapatkan rekomendasi kualitas, harga, dan deskripsi otomatis menggunakan AI">
    <meta name="keywords" content="soto, analyzer, food, AI, upload, foto, kuliner, indonesia">
    <meta name="author" content="Soto Analyzer">
    
    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website">
    <meta property="og:url" content="/">
    <meta property="og:title" content="Soto Analyzer - Analisis Foto Soto dengan AI">
    <meta property="og:description" content="Upload foto soto untuk mendapatkan rekomendasi kualitas, harga, dan deskripsi otomatis">
    
    <!-- Favicon -->
    <link rel="icon" type="image/x-icon" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='.9em' font-size='90'>🍜</text></svg>">
    
    <!-- PWA manifest -->
    <link rel="manifest" href="/manifest.json">
    <meta name="theme-color" content="#ff8c42">
    
    <!-- Styles -->
    <link rel="stylesheet" href="/style.css">
    
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #f5e6d3 0%, #e8d5c4 100%);
            min-height: 100vh;
            padding: 20px;
        }

        .container {
            max-width: 800px;
            margin: 0 auto;
        }

        .header {
            text-align: center;
            margin-bottom: 40px;
            animation: fadeInUp 0.8s ease;
        }

        .header h1 {
            color: #8b4513;
            font-size: 2.5rem;
            margin-bottom: 10px;
            font-weight: 700;
        }

        .header p {
            color: #a0522d;
            font-size: 1.1rem;
            font-weight: 500;
        }

        .upload-container {
            background: white;
            border-radius: 20px;
            padding: 40px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            margin-bottom: 30px;
            animation: fadeInUp 0.8s ease 0.2s both;
        }

        .upload-area {
            border: 3px dashed #ff8c42;
            border-radius: 15px;
            padding: 60px 20px;
            text-align: center;
            transition: all 0.3s ease;
            background-color: #fdf8f5;
            cursor: pointer;
            position: relative;
            overflow: hidden;
        }

        .upload-area::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(255, 140, 66, 0.1) 0%, transparent 70%);
            opacity: 0;
            transition: opacity 0.3s ease;
        }

        .upload-area:hover::before {
            opacity: 1;
        }

        .upload-area:hover {
            border-color: #ff7324;
            background-color: #fcf4ed;
            transform: translateY(-2px);
        }

        .upload-area.dragover {
            border-color: #ff7324;
            background-color: #fff3e6;
            transform: scale(1.02);
        }

        .upload-title {
            color: #8b4513;
            font-size: 1.8rem;
            font-weight: 600;
            margin-bottom: 15px;
            position: relative;
            z-index: 1;
        }

        .upload-subtitle {
            color: #a0522d;
            font-size: 1rem;
            margin-bottom: 30px;
            line-height: 1.5;
            position: relative;
            z-index: 1;
        }

        .upload-btn {
            background: linear-gradient(135deg, #ff8c42 0%, #ff7324 100%);
            color: white;
            border: none;
            padding: 15px 40px;
            border-radius: 30px;
            font-size: 1.1rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 5px 15px rgba(255, 140, 66, 0.3);
            position: relative;
            z-index: 1;
        }

        .upload-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(255, 140, 66, 0.4);
        }

        .upload-btn:active {
            transform: translateY(0);
        }

        #fileInput {
            display: none;
        }

        .preview-container {
            margin-top: 30px;
            display: none;
            animation: slideInRight 0.5s ease;
        }

        .preview-image {
            max-width: 100%;
            max-height: 300px;
            border-radius: 10px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
            transition: transform 0.3s ease;
        }

        .preview-image:hover {
            transform: scale(1.02);
        }

        .result-container {
            background: white;
            border-radius: 20px;
            padding: 30px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            display: none;
            margin-top: 20px;
            animation: fadeInUp 0.8s ease;
        }

        .photo-analysis-section {
            background: #f8f9fa;
            border-radius: 15px;
            padding: 25px;
            margin-bottom: 30px;
            border-left: 4px solid #ff8c42;
        }

        .photo-preview {
            text-align: center;
            margin-bottom: 20px;
        }

        .photo-preview img {
            max-width: 100%;
            max-height: 300px;
            border-radius: 10px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
        }

        .quality-indicators {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
            gap: 15px;
            margin-top: 20px;
        }

        .quality-indicator {
            text-align: center;
            padding: 15px;
            background: white;
            border-radius: 10px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.05);
        }

        .quality-score {
            font-size: 1.5rem;
            font-weight: bold;
            color: #ff8c42;
        }

        .recommendations-section {
            margin: 30px 0;
        }

        .recommendations-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 20px;
            margin-top: 20px;
        }

        .recommendation-card {
            background: white;
            border-radius: 15px;
            overflow: hidden;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
            transition: transform 0.3s ease;
        }

        .recommendation-card:hover {
            transform: translateY(-5px);
        }

        .recommendation-image {
            width: 100%;
            height: 200px;
            object-fit: cover;
        }

        .recommendation-content {
            padding: 20px;
        }

        .merchant-name {
            color: #666;
            font-size: 0.9rem;
            margin-bottom: 5px;
        }

        .rating {
            display: flex;
            align-items: center;
            gap: 5px;
            margin-top: 10px;
        }

        .stars {
            color: #ffc107;
        }

        .pricing-section {
            background: linear-gradient(135deg, #e3f2fd 0%, #f3e5f5 100%);
            border-radius: 15px;
            padding: 25px;
            margin: 30px 0;
        }

        .price-cards {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin-top: 20px;
        }

        .price-card {
            background: white;
            border-radius: 12px;
            padding: 20px;
            text-align: center;
            box-shadow: 0 3px 10px rgba(0,0,0,0.1);
        }

        .price-value {
            font-size: 1.8rem;
            font-weight: bold;
            color: #2e7d32;
            margin: 10px 0;
        }

        .price-label {
            color: #666;
            font-size: 0.9rem;
        }

        .ai-recommended {
            background: linear-gradient(135deg, #4caf50 0%, #45a049 100%);
            color: white;
        }

        .copywriting-section {
            background: linear-gradient(135deg, #fff3e0 0%, #fce4ec 100%);
            border-radius: 15px;
            padding: 25px;
            margin: 30px 0;
        }

        .selling-points {
            list-style: none;
            padding: 0;
            margin: 20px 0;
        }

        .selling-points li {
            padding: 8px 0;
            border-bottom: 1px solid #e0e0e0;
        }

        .selling-points li:last-child {
            border-bottom: none;
        }

        .hashtags {
            margin-top: 15px;
        }

        .hashtag {
            display: inline-block;
            background: #ff8c42;
            color: white;
            padding: 5px 10px;
            border-radius: 20px;
            font-size: 0.8rem;
            margin: 3px;
        }

        .cta-button {
            background: linear-gradient(135deg, #ff8c42 0%, #ff7324 100%);
            color: white;
            border: none;
            padding: 15px 30px;
            border-radius: 25px;
            font-size: 1.1rem;
            font-weight: 600;
            cursor: pointer;
            margin-top: 20px;
            width: 100%;
            transition: all 0.3s ease;
        }

        .cta-button:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(255, 140, 66, 0.4);
        }

        .suggestions-list {
            background: #fff3cd;
            border: 1px solid #ffeaa7;
            border-radius: 8px;
            padding: 15px;
            margin-top: 15px;
        }

        .suggestions-list ul {
            margin: 10px 0;
            padding-left: 20px;
        }

        .suggestions-list li {
            margin: 5px 0;
            color: #856404;
        }

        .result-header {
            text-align: center;
            margin-bottom: 25px;
        }

        .result-title {
            color: #8b4513;
            font-size: 1.5rem;
            font-weight: 600;
            margin-bottom: 10px;
        }

        .confidence-badge {
            background: linear-gradient(135deg, #4CAF50 0%, #45a049 100%);
            color: white;
            padding: 8px 20px;
            border-radius: 25px;
            font-size: 0.9rem;
            font-weight: 600;
            display: inline-block;
            box-shadow: 0 3px 10px rgba(76, 175, 80, 0.3);
        }

        .result-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
            margin-top: 25px;
        }

        .result-card {
            background: #f8f9fa;
            padding: 25px;
            border-radius: 15px;
            border-left: 4px solid #ff8c42;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .result-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 35px rgba(0,0,0,0.15);
        }

        .result-card h3 {
            color: #8b4513;
            font-size: 1.1rem;
            margin-bottom: 10px;
            font-weight: 600;
        }

        .result-card p {
            color: #666;
            line-height: 1.5;
        }

        .loading {
            display: none;
            text-align: center;
            padding: 20px;
        }

        .spinner {
            width: 40px;
            height: 40px;
            border: 4px solid #f3f3f3;
            border-top: 4px solid #ff8c42;
            border-radius: 50%;
            animation: spin 1s linear infinite;
            margin: 0 auto 15px;
        }

        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes slideInRight {
            from {
                opacity: 0;
                transform: translateX(30px);
            }
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        .error-message {
            background: #ffe6e6;
            color: #d32f2f;
            padding: 15px;
            border-radius: 10px;
            margin-top: 15px;
            border-left: 4px solid #d32f2f;
            display: none;
        }

        .success-message {
            background: #e8f5e8;
            color: #2e7d32;
            padding: 15px;
            border-radius: 10px;
            margin-top: 15px;
            border-left: 4px solid #2e7d32;
            display: none;
        }

        .file-info {
            background: #f0f8ff;
            padding: 20px;
            border-radius: 10px;
            margin-top: 15px;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 15px;
        }

        .file-info-item {
            background: white;
            padding: 15px;
            border-radius: 8px;
            border-left: 4px solid #ff8c42;
            box-shadow: 0 2px 8px rgba(0,0,0,0.05);
        }

        .file-info-item strong {
            color: #8b4513;
            display: block;
            margin-bottom: 5px;
            font-size: 0.9rem;
        }

        .progress-bar {
            width: 100%;
            height: 6px;
            background: #f0f0f0;
            border-radius: 3px;
            overflow: hidden;
            margin-top: 15px;
            display: none;
        }

        .progress-fill {
            height: 100%;
            background: linear-gradient(90deg, #ff8c42, #ff7324);
            width: 0%;
            transition: width 0.3s ease;
        }

        .nav-button {
            position: fixed;
            top: 20px;
            right: 20px;
            background: #ff8c42;
            color: white;
            border: none;
            padding: 12px 20px;
            border-radius: 25px;
            cursor: pointer;
            font-weight: 600;
            box-shadow: 0 5px 15px rgba(255, 140, 66, 0.3);
            transition: all 0.3s ease;
            z-index: 1000;
        }

        .nav-button:hover {
            background: #ff7324;
            transform: translateY(-2px);
        }

        .modal {
            display: none;
            position: fixed;
            z-index: 2000;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0,0,0,0.8);
            animation: fadeIn 0.3s ease;
        }

        .modal-content {
            position: relative;
            background-color: white;
            margin: 5% auto;
            padding: 30px;
            border-radius: 20px;
            width: 90%;
            max-width: 800px;
            max-height: 80vh;
            overflow-y: auto;
            animation: slideInUp 0.3s ease;
        }

        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        @keyframes slideInUp {
            from {
                opacity: 0;
                transform: translateY(50px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .close {
            position: absolute;
            right: 20px;
            top: 15px;
            color: #aaa;
            font-size: 28px;
            font-weight: bold;
            cursor: pointer;
            transition: color 0.3s ease;
        }

        .close:hover,
        .close:focus {
            color: #8b4513;
        }

        @media (max-width: 768px) {
            .container {
                padding: 10px;
            }
            
            .header h1 {
                font-size: 2rem;
            }
            
            .upload-container {
                padding: 20px;
            }
            
            .upload-area {
                padding: 40px 15px;
            }
            
            .upload-title {
                font-size: 1.5rem;
            }
            
            .result-grid {
                grid-template-columns: 1fr;
            }
            
            .nav-button {
                top: 10px;
                right: 10px;
                padding: 8px 15px;
                font-size: 0.9rem;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Upload foto soto Anda untuk mendapatkan rekomendasi kualitas, harga, dan deskripsi otomatis.</h1>
        </div>

        <div class="upload-container">
            <div class="upload-area" id="uploadArea" tabindex="0" role="button" aria-label="Area upload foto soto">
                <h2 class="upload-title">Upload Foto Soto Anda</h2>
                <p class="upload-subtitle">Seret dan lepas foto soto di sini, atau klik untuk memilih file</p>
                <button class="upload-btn" onclick="document.getElementById('fileInput').click()">
                    Pilih File Foto
                </button>
                <input type="file" id="fileInput" accept="image/*" aria-label="Pilih file gambar soto">
            </div>

            <div class="preview-container" id="previewContainer">
                <img id="previewImage" class="preview-image" alt="Preview foto soto">
                <div class="file-info" id="fileInfo"></div>
            </div>

            <div class="loading" id="loading">
                <div class="spinner"></div>
                <p>Menganalisis foto soto Anda...</p>
            </div>

            <div class="error-message" id="errorMessage" role="alert"></div>
            <div class="success-message" id="successMessage" role="alert"></div>
        </div>

        <div class="result-container" id="resultContainer">
            <!-- 1. Photo Analysis Section -->
            <div class="photo-analysis-section" id="photoAnalysisSection">
                <h3>📸 Analisis Kualitas Foto</h3>
                <div class="photo-preview" id="resultPhotoPreview">
                    <!-- Foto akan dimuat di sini -->
                </div>
                <div id="photoQualityFeedback">
                    <!-- Feedback kualitas foto -->
                </div>
                <div class="quality-indicators" id="qualityIndicators">
                    <!-- Indikator kualitas -->
                </div>
                <div class="suggestions-list" id="photoSuggestions">
                    <!-- Saran perbaikan foto -->
                </div>
            </div>

            <!-- 2. Soto Recommendations Section -->
            <div class="recommendations-section" id="recommendationsSection">
                <h3>🍜 Rekomendasi Foto Soto Terbaik</h3>
                <p>Berikut adalah contoh foto soto dengan kualitas terbaik dari merchant yang sudah terverifikasi AI:</p>
                <div class="recommendations-grid" id="recommendationsGrid">
                    <!-- Rekomendasi foto soto -->
                </div>
            </div>

            <!-- 3. Price Recommendation Section -->
            <div class="pricing-section" id="pricingSection">
                <h3>💰 Rekomendasi Harga AI</h3>
                <p id="priceReasoning"><!-- Reasoning harga dari AI --></p>
                <div class="price-cards" id="priceCards">
                    <!-- Kartu harga -->
                </div>
            </div>

            <!-- 4. AI Copywriting Section -->
            <div class="copywriting-section" id="copywritingSection">
                <h3>✨ Deskripsi & Copywriting AI</h3>
                <div id="aiCopywritingContent">
                    <!-- Konten copywriting AI -->
                </div>
                <div class="hashtags" id="hashtagsContainer">
                    <!-- Hashtags -->
                </div>
                <button class="cta-button" id="ctaButton">
                    <!-- Call to action -->
                </button>
            </div>
        </div>
    </div>

    <!-- Include external JavaScript -->
    <script src="/app.js"></script>
</body>
</html>
